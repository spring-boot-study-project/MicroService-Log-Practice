server:
    port: 8080

spring:
    main:
        web-application-type: servlet
        
    profiles:
        active: dev
    
    application:
        name: microservice-1
    
    config:
        import:
            - security/application-db.yml

kafka:
  bootstrap:
    servers: localhost:9092
