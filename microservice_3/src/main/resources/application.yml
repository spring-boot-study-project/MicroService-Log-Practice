server:
    port: 8083

spring:
    main:
        web-application-type: servlet
        
    profiles:
        active: dev
    
    application:
        name: microservice-3
    
    config:
        import:
            - security/application-db.yml

kafka:
  bootstrap:
    servers: localhost:9092
