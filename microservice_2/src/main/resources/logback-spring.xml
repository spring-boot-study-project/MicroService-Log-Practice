<configuration>
    <springProperty scope="context" name="springAppName" source="spring.application.name"/>
    <springProperty scope="context" name="kafkaBootstrapServers" source="kafka.bootstrap.servers" defaultValue="localhost:9092"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="LOGSTASH" class="com.github.danielwegener.logback.kafka.KafkaAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <providers>
                <timestamp/>
                <version/>
                <message/>
                <loggerName/>
                <threadName/>
                <logLevel/>
                <callerData/>
                <stackTrace/>
                <mdc/> <!-- To include MDC properties like traceId -->
                <springAppName/> <!-- Custom provider for spring.application.name -->
            </providers>
        </encoder>
        <topic>service-logs</topic>
        <keyingStrategy class="com.github.danielwegener.logback.kafka.keying.NoKeyKeyingStrategy"/>
        <deliveryStrategy class="com.github.danielwegener.logback.kafka.delivery.AsynchronousDeliveryStrategy"/>
        <producerConfig>
            <bootstrap.servers>\${kafkaBootstrapServers}</bootstrap.servers>
            <!-- Add other Kafka producer properties as needed -->
            <!-- Example:
            <acks>1</acks>
            <retries>0</retries>
            <security.protocol>PLAINTEXT</security.protocol>
            -->
        </producerConfig>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="LOGSTASH"/>
    </root>

    <!-- Optional: More specific loggers -->
    <logger name="org.springframework" level="INFO"/>
    <logger name="com.springboot.microservice_2" level="DEBUG"/> <!-- Adjust your base package -->

</configuration>
